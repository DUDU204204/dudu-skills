# ערכת הסקילים - הוראות לקלוד

התיקייה הזו היא אוסף סקילים ל-Claude Code בעברית, לבעלי עסקים. אחרי `./install.sh` הם יושבים
ב-`~/.claude/skills/` וזמינים בכל שיחה.

## אם המשתמש רק התקין

1. הרץ `./install.sh` והצג לו מה הותקן.
2. שאל אותו **מה הוא עושה הכי הרבה** (תוכן / וידאו / מכירות / דפים), והצע להתחיל מסקיל אחד -
   לא מכולם. סקיל אחד שרץ פעמיים עדיף על 16 שמותקנים ולא בשימוש.
3. אם הוא בחר סקיל שדורש כלי חיצוני (ffmpeg, מודל תמלול, מפתח API) - בדוק שהוא קיים, והתקן או
   הנחה. הכללים: מפתחות ב-`~/.config/<כלי>/` עם `chmod 600`, לעולם לא בתיקיית העסק ולא בצ'אט.
4. הצע לבנות את `06-מותג/קול.md` דרך `/voice-write` - הוא משפר כל סקיל כתיבה אחר.

## חוקי ברזל

1. **לא ממציאים עובדות בקופי.** מספר, תאריך, סיפור לקוח או תוצאה שלא נאמרו בחומר הגלם - לא
   נכנסים. חסר נתון: `[חסר: ...]` ולשאול.
2. **סקיל שכותב + סקיל שבודק.** אחרי כל סקיל כתיבה - `/copy-supervisor`.
3. **תוצאה לא טובה = מעדכנים את הסקיל**, אחרי אישור הניסוח מהמשתמש. אף פעם לא לשכתב סקיל בלי
   שהוא ראה את השינוי.
4. **הסקילים גנריים בכוונה.** שם לקוח, נתיב אישי או מפתח שנכנסים לסקיל - הופכים אותו ללא-שמיש
   לאף אחד אחר, כולל למשתמש בעוד חצי שנה. דוגמאות נשארות דוגמאות.
5. מקף רגיל `-`, בלי em dash. עברית = RTL בכל תוצר (מייל: `dir="rtl"` על div פנימי).

## מבנה

`skills/<name>/SKILL.md` - הסקיל עצמו (frontmatter: `name`, `description`, `user-invocable`).
חלק מהסקילים כוללים גם סקריפט פייתון לצידם; הם מתועדים בתוך ה-SKILL.md.
